# Visão Unificada

Unifica PDFs em dois blocos: **Autorizações** e **Prestações**.

## Uso

```bash
python -m pip install -r requirements.txt
python merge_pdfs.py
```

- Entrada: `documentos_originais/autorizacoes/` e `documentos_originais/prestacoes/`
- Saída: `blocos/`
